# NOTE: xpad is built into the kernel (CONFIG_JOYSTICK_XPAD=y), so this file
# has no effect. auto_poweroff is disabled at runtime via S13octane-init using
# /sys/bus/usb/drivers/xpad/module/parameters/auto_poweroff.
# options xpad auto_poweroff=N
